# SPEC 收口清单：20 项以内

日期：2026-09-19。

owner 要求 grill with docs 总问题不超过 20 个。此前原始提问确已超过 20（含撤回／重问），不再新增访谈问题。owner 于 2026-09-19 进一步判定：D18～D20 粒度过细，要求按大方向收拢，核心是 Web 上的用户体验——**左侧聊天列表、中间对话、右侧文件树、底部输入框与 Gitea 分支选择**，已写入主 SPEC §1.1。因此 D18～D20 全部降级为实现细节，不再作为 owner 决策题。编号是文档索引，不是伪称历史只问了 20 次。

完整过程见 [访谈记录](spec-grill-20260917.md)，当前架构见 [ADR-0010](../adr/0010-unified-web-gateway-private-user-vms.md)，主合同见 [多用户 VM SPEC](../spec/multi-user-vm.md)。

| 索引 | 议题 | 已确认内容／当前状态 |
|---|---|---|
| D01 | 产品与拓扑 | 已确认：Arena Agent 式统一网页体验；统一 HTTPS 网关连接每用户私有 VM，不逐 VM 开公网入口。 |
| D02 | 能力与执行归属 | 已确认：Web 是 Pi 用户界面；原生 Pi 历史、Git/worktree 和已有插件优先；必要薄适配，不重建 Agent。常用操作按钮化，复杂开发／Git 交给同一个 Pi；冲突处理先用户确认，完成后重新预览和确认合并。 |
| D03 | 部署接入 | 已确认：幂等安装／升级脚本、手工 VM 与路由管理；不做一次性 Enrollment 服务。升级细节由实现方案交代，不冒充已确认自动中断或恢复策略。 |
| D04 | 运维入口 | 已确认：诊断命令／报告，不开发网页管理后台。 |
| D05 | 数据与文件通道 | 已确认：VM 保存权威历史和文件；文件经同源网关有界流式转发，不在网关存正文；Relay 独立可选，集中密钥不下放 VM。 |
| D06 | 缓存位置 | 已确认：仅浏览器缓存，不新建中央聊天数据库。 |
| D07 | 缓存内容 | 已确认：用户／助手正文及简短工具摘要，不缓存完整工具输出、附件原件或授权 Token。 |
| D08 | 缓存预算 | 已确认：每账号默认 200 MiB／20000 条，达到任一即淘汰，可调整，不另限对话数量。 |
| D09 | 热度与预取 | 已确认：VM 成功接受用户消息才刷新 7 天计时；最多 5 个近期未归档对话各预取最新 100 条。冷对话仍同步必要任务状态。 |
| D10 | 缓存释放 | 已确认：超过 7 天可启动／空闲清理；不打断当前阅读；容量不足优先淘汰最久未访问内容。 |
| D11 | 身份与缓存展示 | 已确认：退出保留缓存，但身份验证后才展示；账号隔离，不额外设计本地加密／设备认证。 |
| D12 | VM 离线 | 已确认：只读缓存、编辑文字草稿；不排队执行离线操作；重连同步后用户明确发送。 |
| D13 | 换绑与历史删除 | 已确认：确认 VM 环境改绑后清旧环境缓存；VM 明确删除历史后同步清缓存，网络失败不是删除证据。 |
| D14 | 完整历史与阅读 | 已确认：缓存先显示、原生历史增量核对，上滚按需加载、保留阅读位置、有界渲染。先前“切回运行对话是否跟随底部”的细分问法未获回答，不新增强制行为；遵守不强制打断阅读的已确认原则。 |
| D15 | 草稿 | 已确认：本地文字草稿按账号／环境／对话保留，刷新／退出后恢复；清缓存默认不清草稿；确认接收后只清发送版本。 |
| D16 | 归档 | 已确认：主 Pi／受管子任务运行、排队或状态未知时拒绝归档，不自动停止或预约归档。 |
| D17 | 删除 | 已确认：归档页明确确认，默认只删历史；工作区清理单独勾选，仅干净且已合并时允许；保留分支／上传及资源登记。 |
| D18 | 保留资源整理 | 降级为实现细节（owner 2026-09-19 判定粒度过细）：不静默删除用户成果，按 §1.1 骨架复用现有资源呈现；不再作为决策题。 |
| D19 | 操作中断恢复 | 降级为实现细节：恢复不得自动续跑破坏性操作，未知状态明确报告；不再作为决策题。 |
| D20 | 运行中发送默认 | 降级为实现细节：排队与插话两种能力都保留（`#mode` 已存在），默认值由实现决定；不再作为决策题。 |

## 核心方向（owner 2026-09-19）

界面即产品合同，详见主 SPEC §1.1。所有后续实现以「左列表 / 中对话 / 右文件树 / 底输入条＋分支选择」为验收基准；与 §1.1 冲突的旧浮层式文件面板和左栏起始分支输入需调整。

## 后续处理

- 不再追加新的访谈题目；原始超出题数的事实不通过重编号抹去。
- D18～D20 已按 owner 要求降级，不再重新提问，也不得把原建议当成已批准要求写入代码注释。
- 对 D18～D20 仅收集已有未决项的确认／修改，不扩展新的决策分支。
- D01～D17 中已确认的领域规则同步到主 SPEC；其余实现细节优先核查现有代码／公共接口，并明确区分工程选择与 owner 产品决定。
- D01～D20 全部关闭：17 项已确认，3 项降级为实现细节。SPEC 可据此进入实现；实机测试单独编写本地任务书。
